import OptionsView from './list/options-view';
import PickerView from './picker/picker-view';

// const options = new OptionsView();
// document.body.append(options.getNode());
const picker = new PickerView();
document.body.append(picker.getNode());
